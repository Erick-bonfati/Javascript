<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Testes</title>
</head>
<body>
  <h1>

    <% if(typeof titulo !== 'undefined') { %> <!-- Abrimos e fechamos tags para realizar controles de fluxo no HTML -->
      <%= titulo %> <!-- USAMOS O SINAL DE IGUAL PARA DIZER QUE QUEREMOS EXIBIR UMA VARIAVEL NA TELA -->
    <% } else {  %> <!-- ABERTURA E FECHAMENTO DE TAGS COM EJS FUNCIONA ASSIM ( REACT É BEM PARECIDO COM ISSO, POREM SINTAXE DIFERENTE) -->
      Titulo não foi enviado.
    <% } %>

  </h1>

  <form action="/" method="POST">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>"> <!-- Campo oculto para o token CSRF -->
    <input type="text" name="nome" placeholder="Seu nome">
    <button>Enviar</button>
  </form>

</body>
</html>